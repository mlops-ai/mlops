import { DropdownMenuLabel } from "@/components/ui/dropdown-menu";

const ModelMenuHeader = () => {
    return (
        <DropdownMenuLabel>
            <div className="flex items-center font-semibold">
                Model actions
            </div>
        </DropdownMenuLabel>
    );
};

export default ModelMenuHeader;
